/* ============================================================
 * ticker.worker.js — 闹钟后台心跳（Web Worker）
 * 浏览器会对后台标签页的主线程定时器节流（可降至 1 分钟/次），
 * 而 Worker 定时器通常仍按秒运行。用它保证闹钟在后台也能准点触发。
 * 依赖：无
 * ============================================================ */
'use strict';

setInterval(function () {
  postMessage('tick');
}, 1000);
